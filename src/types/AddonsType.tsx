interface AddonsPropsType {
    active: boolean
    price: number
}

export interface AddonsType {
    onlineService: AddonsPropsType
    largerStorage: AddonsPropsType
    customProfile: AddonsPropsType
}